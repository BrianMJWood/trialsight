import { favouritesDataAccess } from './favourites-data-access';

describe('favouritesDataAccess', () => {
  it('should work', () => {
    expect(favouritesDataAccess()).toEqual('favourites-data-access');
  });
});
